#include<stdio.h>
int main(){
	int x=15, y=6;
	int a =x/y, b=x%y;
	printf("a=%d\tb=%d\n",a,b);
// the mod only work for int and char in c language and not applicable for float and double

	return 0;
}
